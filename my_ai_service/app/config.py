from pathlib import Path

# 현재 app 디렉토리
APP_DIR = Path(__file__).parent

# ==========================================
# [설정] 모델 경로 (weights가 app 폴더 안에 있음)
# ==========================================
DET_MODEL_PATH = APP_DIR / "weights" / "det_best.pt"  # 객체 탐지 모델
CLS_MODEL_PATH = APP_DIR / "weights" / "cls_best.pt"  # 분류 모델
SAM_MODEL_PATH = APP_DIR / "weights" / "FastSAM-s.pt"  # 세그멘테이션 모델 (FastSAM)

# ==========================================
# [설정] YOLO 클래스 ID (모델 학습 시 설정한 순서와 일치해야 함)
# ==========================================
CLASS_ID_BASIL = 0   # 바질/식물
CLASS_ID_SCALE = 1   # 스케일 마커

# ==========================================
# [설정] Scale(기준 스티커) 실제 크기
# ==========================================
SCALE_REAL_DIAMETER_MM = 16.0  # 스티커의 실제 지름: 16mm

# ==========================================
# [설정] 초록색 인식 범위 (HSV)
# ==========================================
GREEN_HSV_LOWER = [35, 40, 40]    # 초록색 하한
GREEN_HSV_UPPER = [85, 255, 255]  # 초록색 상한

# ==========================================
# [설정] 신뢰도 임계값
# ==========================================
CONFIDENCE_THRESHOLD = 0.5  # 탐지 신뢰도
